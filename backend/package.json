{
  "name": "cando",
  "version": "1.0.0",
  "description": "Educational platform Cando",
  "main": "backend/server.js",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "18.x"
  }
}
